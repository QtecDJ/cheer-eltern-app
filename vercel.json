{
  "buildCommand": "prisma generate && next build",
  "framework": "nextjs",
  "installCommand": "npm install",
  "regions": ["fra1"],
  "functions": {
    "src/app/**/*.ts": {
      "maxDuration": 10
    },
    "src/app/**/*.tsx": {
      "maxDuration": 10
    }
  }
}
