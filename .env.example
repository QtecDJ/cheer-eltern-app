# Datenbank (Neon PostgreSQL)
DATABASE_URL="postgresql://username:password@host/database?sslmode=require"

# Session Secret (generiere einen sicheren Schlüssel, z.B. mit: openssl rand -base64 32)
SESSION_SECRET="your-secret-key-here-min-32-characters"

# Push Notifications - VAPID Public Key (optional placeholder; *do not* commit real keys)
NEXT_PUBLIC_VAPID_PUBLIC_KEY="<YOUR_VAPID_PUBLIC_KEY_OR_EMPTY>"

# Optional: Node Environment
NODE_ENV="production"
