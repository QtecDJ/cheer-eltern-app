export function useIsIOSResuming(key: string, windowMs: number = 5000): boolean {
  // archived implementation
  return false;
}

export function triggerIOSResume(key: string): void {
  // archived
}

export const MyContent = null as any;
